/**
 * The working name, per `docs/PRODUCT.md`. Q-001 settles the final one; until
 * then this constant is the single place it is written down, so renaming is one
 * edit rather than a search.
 */
export const APP_NAME = 'Loft';

export const APP_SLUG = 'loft';

export const BUNDLE_ID = 'app.loft.pidge';
