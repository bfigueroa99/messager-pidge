{
  "name": "@pidge/flight-sim",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, deterministic, dependency-free flight physics. Runs identically in React Native, Deno and Node.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b"
  }
}
